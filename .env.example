# Copy to .env.local and fill in. The key is read server-side only.
ANTHROPIC_API_KEY=sk-ant-...
# Optional: override the model used for generation + classification.
# ANTHROPIC_MODEL=claude-sonnet-4-6
